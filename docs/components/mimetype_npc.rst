MimeType NPC
============

The MimeType NPC uses the python-magic library to determine the mime type of a given binary file. It then creates a ``MimeTypeFact`` with that information. This NPC takes no configuration.
